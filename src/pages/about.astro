---
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';
const pageTitle = 'About Me';

const identity = {
  firstName: 'Alok',
  country: 'India',
  occupation: 'Frontend Developer',
  hobbies: ['Checking Tweets', 'Working out', 'Cooking'],
};

const skills = ['HTML', 'CSS', 'JavaScript', 'React', 'Astro', 'Writing Blogs'];

const finished = false;
const goal = 3;

const skillColor = 'navy';
const fontWeight = 'bold';
const textCase = 'uppercase';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <style define:vars={{ skillColor, fontWeight, textCase }}>
      h1 {
        color: purple;
        font-size: 4rem;
      }
      .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
      }
    </style>
  </head>
  <body>
    <Navigation />
    <h1>Hey! I'm {identity.firstName} 👋</h1>
    <h2>Welcome to my new Astro site!</h2>

    <p>
      I'm currently working through Astro's introductory tutorial. This is the
      second page on my site — and the first one I built all by myself!
    </p>

    <p>
      This site will grow as I learn more, so feel free to check back and follow
      along with my progress.
    </p>

    <p>
      I'm based in {identity.country} and work as a {identity.occupation}.
      {
        identity.hobbies.length >= 2 && (
          <>
            {' '}
            During my freetime, I enjoy {identity.hobbies[0].toLowerCase()} and{' '}
            {identity.hobbies[1].toLowerCase()}.
          </>
        )
      }
    </p>

    <h3>What I'm learning right now:</h3>
    <ul>
      {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>

    {finished && <p>I finished this tutorial!</p>}
    {
      goal === 3 ? (
        <p>My goal is to finish in {goal} days.</p>
      ) : (
        <p>My goal is not {goal} days.</p>
      )
    }
    <Footer />
  </body>
</html>
